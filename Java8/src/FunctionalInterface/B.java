package FunctionalInterface;


//@FunctionalInterface
public interface B extends A {      //here B extends A and also it is functional Interface so it have
	 void display();                  // only method of A not own because only one method is allowed

}
